package:
  name: sa_uvm_sb
  description: "SA UVM Scoreboard"
  authors:
    - "raymondngarcia"

dependencies:
  uvm:                  { path: "../../../uvm_pkg"}

sources:
  - target: simulation
    include_dirs:
      - .
    files:
      - sa_sb_pkg.sv