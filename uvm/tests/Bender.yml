package:
  name: sa_uvm_test
  description: "Tests for SA UVM Testbench"
  authors:
    - "raymondngarcia"

dependencies:
  sa_uvm_seq:            { path: "../sequences" }

sources:
  - target: simulation
    include_dirs:
      - .
    files:
      - sa_test_pkg.sv
