package:
  name: sa_uvm_agent
  description: "SA UVM Agent"
  authors:
    - "raymondngarcia"

dependencies:
  uvm        :    { path: "../../../uvm_pkg"}
  sa_rtl     :    { path: "../../../rtl"  } # needs the interface

sources:
  - target: simulation
    include_dirs:
      - .
    files:
      - sa_agent_pkg.sv
